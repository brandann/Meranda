using UnityEngine;
using System.Collections;

public class ground : MonoBehaviour {

	// Use this for initialization
	void Start () {
		transform.position = new Vector3 (-0f, -12.5f, -5f);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
